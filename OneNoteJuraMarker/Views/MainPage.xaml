<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OneNoteJuraMarker.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneNoteJuraMarker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="using:OneNoteJuraMarker.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid x:Name="AppTitleBar">
            <ComboBox DisplayMemberPath="Name" ItemsSource="{Binding Notebooks}" Margin="0,20,0,0" PlaceholderText="Select the OneNote Sheet you'd like to edit" Height="30" Width="500" HorizontalAlignment="Center"></ComboBox>
            <StackPanel Margin="0,70,0,0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" >
                <Button Command="{Binding  LoadNoteBooksCommand}">ShowPreview</Button>
            </StackPanel>
            <TreeView x:Name="NotebookTreeView"
                  Width="400"
                  Margin="50"/>
        </Grid>
        
    </Grid>
</Page>
